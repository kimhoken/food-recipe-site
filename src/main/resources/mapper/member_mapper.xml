<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.foodsite.dao.MemberDAO">
	
    <select id="selectList" resultType="member">
        select * from member 
    </select>

    <select id="getUserById" 
            parameterType="String"
            resultType="member">
        select * from member where login_id = #{id}
    </select>        

    <insert id="userInsert" parameterType="member">
        insert into member (login_id, password, nickname, email, role, status, report_count, created_date, profile_img, login_type, provider, provider_id,name)         
        values (#{login_id}, #{password}, #{nickname}, #{email}, #{role}, #{status}, 0, sysdate(), #{filename}, #{login_type}, #{provider}, #{provider_id}, #{name})
    </insert>

    <select id="getUserNickname"
            parameterType="String"
            resultType = "member">
        select * from member where nickname= #{nickname}
    </select>        

    <select id="getUserEmail"
            parameterType="String"
            resultType = "member">
        select * from member where email = #{email}
    </select>      

    <select id="getUserByMemberId"
            parameterType="int"
            resultType = "member">
        select * from member where member_id = #{member_id}
    </select>   

    <update id="userPwdUpdate" parameterType="member">
        update member set password = #{password}
        where member_id = #{member_id}
    </update>    

    <select id="getSocialUser"
            parameterType="String"
            resultType="member">
        select * from member
        where provider = #{provider} and
        provider_id = #{provider_id}
    </select>

    <update id="userUpdate" parameterType="member">
        update member set nickname = #{nickname}, email = #{email}, profile_img = #{filename}, name = #{name}, member_intro = #{member_intro}
        where member_id = #{member_id}
    </update>

    <update id="secessionUser" parameterType="member">
        update member set login_id = #{login_id}, password = #{password}, status =#{status}, nickname = #{nickname}, email=#{email}, profile_img = #{profile_img}, name = #{name}, member_intro = #{member_intro} 
        where member_id = #{member_id}
    </update>
</mapper>