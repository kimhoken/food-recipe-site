<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.foodsite.dao.MemberDAO">
	
    <select id="selectList" resultType="member">
        select * from member 
    </select>

    <select id="getUserById" 
            parameterType="String"
            resultType="member">
        select * from member where login_id = #{id}
    </select>        

    <insert id="userInsert" parameterType="member">
        insert into member (login_id, password, nickname, email, role, status, report_count, created_date, profile_img, name)         
        values (#{login_id}, #{password}, #{nickname}, #{email}, #{role}, #{status}, 0, sysdate(), #{filename}, #{name})
    </insert>

    <select id="getUserNickname"
            parameterType="String"
            resultType = "member">
        select * from member where nickname= #{nickname}
    </select>        

    <select id="getUserEmail"
            parameterType="String"
            resultType = "member">
        select * from member where email = #{email}
    </select>        

</mapper>