<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.foodsite.dao.CategoryDAO">
    
    <select id="foodListCategory" parameterType="String" resultType="food">
        SELECT 
            f.food_id,
            f.food_name,
            f.category_id
        FROM food f
        INNER JOIN category c ON f.category_id = c.category_id
        WHERE c.category_name = #{category}
        ORDER BY f.food_id ASC
    </select>
        
    <select id="foodListAll" resultType="food">
        SELECT food_id, food_name, category_id 
        FROM food
        ORDER BY food_id ASC
    </select>

</mapper>